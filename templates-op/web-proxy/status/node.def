#
# Operational mode front-end for "ping" command
#

help:   Check the status of web proxy service

run:	
	squid_version=`sudo /usr/sbin/squid3 -v | grep "Squid"`
	echo $squid_version
	if [[ ! -z $(ps -ef | grep -v "grep" | grep "(squid)"|awk '{print $2}')  ]]; 
	then
		echo "the Webproxy is running with PID = "  $(ps -ef | grep -v "grep" | grep "(squid)"|awk '{print $2}')
	else
		echo "the Webproxy is stopping"
	fi
