help: Show contents of webproxy access log
run:  if [ -e /var/log/squid3/access.log ]; then
        sudo less $_vyatta_less_options    \
	  --prompt=".log?m, file %i of %m., page %dt of %D" \
	  -- `printf "%s\n" /var/log/squid3/access.log | sort -nr`
      else
        echo No access log
      fi
