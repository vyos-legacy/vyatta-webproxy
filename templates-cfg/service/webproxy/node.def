priority: 917
help: Configure webproxy services

end: if [ ${COMMIT_ACTION} = 'DELETE' ]; then
        sudo /opt/vyatta/sbin/vyatta-update-webproxy.pl --stop;	
     else
        sudo /opt/vyatta/sbin/vyatta-update-webproxy.pl --update;
     fi;
